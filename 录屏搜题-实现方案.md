# 录屏搜题（iPhone 悬浮窗自动扫描）实现方案

> 目标：在现有 Flutter 题库 App 上，新增「录屏搜题」能力——App 内点「开始扫描」→ 系统确认一次 → 之后全自动：系统把屏幕视频帧喂给扩展 → OCR 识别题目 → 悬浮窗（PiP）直接显示答案，浮在考试界面之上。

---

## 0. 先说清三个 iPhone 硬约束（方案成立的前提）

1. **iPhone 没有安卓式全局悬浮窗。** iOS 沙盒禁止第三方 App 在其他 App 之上画持久浮层。竞品 iPhone 版的「悬浮窗」底层是 **画中画 PiP 小窗**——这是 iOS 唯一能浮在别的 App 上的机制。本方案的「悬浮窗」= PiP 小窗（可拖动、常驻、显示答案）。
2. **开录屏必须有一次系统确认。** 苹果隐私强制：任何 App 都不能偷偷开屏幕录制。流程是 App 内点「开始扫描」→ 弹出系统录制选择器 → 用户点一次「开始直播」。这一步去不掉，但就在 App 内点一下，不是每题手动截图。
3. **iOS 15+ 才支持「UIView 内容的 PiP」**（`AVPictureInPictureController(contentSource:)` + `activeVideoCallSourceView`）。目标设备需 iOS 15+（你 iPhone 一般满足）。低于此版本此方案不可行。

> 结论：你要的「点击开始 → 自动开录屏 → 悬浮窗给答案」**完全可行**，只是「悬浮窗」在 iPhone 上就是 PiP 小窗，「自动」从那次系统确认之后开始。

---

## 1. 整体架构与数据流

```
[考试 App 界面]
      │ 屏幕画面被系统录制
      ▼
[iOS 系统] ──视频帧──▶ [Broadcast Upload Extension]  (独立进程，录屏期间一直活着)
      │                        │
      │                        │ 1) Vision 框架 OCR（节流：每 ~1s 或画面变化才识别）
      │                        │ 2) 把识别到的题目文字写入 App Group 共享区
      │                        ▼
      │              [App Group 共享文件/UserDefaults]
      │                        ▲
[主 App（PiP 常驻）] ──轮询──┘
      │ 3) 读取 OCR 文字 → 跑相似度搜索（复用现有 Dart 逻辑）
      │ 4) 把 top 答案写入 App Group 的「答案」字段
      ▼
[PiP 悬浮小窗] 实时渲染答案文字（题目 + 正确答案 + 高亮选项）
```

- **Extension（扩展）**：只负责「收帧 + OCR + 写共享区」，不做匹配，保持轻量、省电。
- **主 App（PiP 常驻）**：负责「读共享区 → 匹配 → 写答案 → 渲染到 PiP」。复用现有 `search.dart` 的 `SearchIndex` / `similarity()`，题库解析结果在「开始扫描」时序列化进 App Group 供主 App 读取。
- **匹配放主 App 而非扩展**，原因：现有搜索/索引逻辑是 Dart，放主 App 直接复用，不重写第二套；PiP 会让主 App 保持活跃足以跑轮询定时器（视频通话类 PiP 应用均如此；实现时实测，若不达标则回退为「扩展内 Swift 轻量匹配」）。

---

## 2. 需新增 / 修改的文件清单

### 新增（iOS 原生，不在 Dart 侧）
| 文件 | 作用 |
|---|---|
| `app/ios/Runner/BroadcastUploadExtension/` 目录 | 新建 **Broadcast Upload Extension** 目标（Xcode target） |
| `BroadcastUploadExtension.swift` | `sampleHandler` 收 `CMSampleBuffer` → Vision OCR → 写 App Group |
| `AppGroup.swift`（共享） | App Group 标识符常量 + 读写辅助（主 App 与扩展共用） |
| `PipViewController.swift` | PiP 悬浮窗内容视图（显示答案的 UIView） |
| `Info.plist`（扩展） | 扩展的 App Group  entitlements 引用 |
| `Runner.entitlements` / `BroadcastUploadExtension.entitlements` | 开启 App Group（如 `group.com.<you>.qbank`） |

### 修改
| 文件 | 改动 |
|---|---|
| `app/ios/Runner/Info.plist` | 开启 **Picture in Picture** 后台模式（`UIBackgroundModes` 加 `audio`? 实测确认；PiP 需项目开启 "Picture in Picture" Capability）；加录屏/辅助说明键 |
| `app/ios/Runner/AppDelegate.swift` | 提供方法：启动 PiP（`AVPictureInPictureController(contentSource: activeVideoCallSourceView:)`）、弹出 `RPSystemBroadcastPickerView`、桥接 Flutter 方法通道 |
| `app/lib/services/scan_bridge.dart`（新增 Dart） | 平台通道封装：`startScan()` / `stopScan()` / 监听答案流 |
| `app/lib/screens/search_screen.dart` | 加「开始扫描 / 停止扫描」按钮 + 扫描状态（红点/计时）；保留原「粘贴搜题」作为兜底 |
| `app/lib/services/bank_store.dart` | 「开始扫描」时把当前题库序列化为 JSON 写入 App Group，供主 App 匹配读取 |
| `.github/workflows/build.yml` | 确保 `flutter build ios` 把 Broadcast Upload Extension 目标一起编（扩展随 Xcode workspace 编译，通常自动包含；需核对签名用 `--no-codesign` 对扩展同样生效） |
| `安装到iPhone-完整步骤.md` | 补充「录屏搜题」使用段：开扫描→系统确认→悬浮窗看答案→停止；以及 iOS 15+ 要求 |

> 注：因涉及 Xcode target / entitlements，**云端 CI 的 `flutter create --platforms=ios` 会重建 iOS 工程并覆盖自定义原生文件**。需在 CI 里把我们新增的原生文件 + entitlements + Info.plist 改动做成「备份→create→还原+补丁」流程（同现有 `lib` 备份思路），或在 `create` 后脚本化写入这些文件。

---

## 3. 各模块实现要点

### 3.1 Broadcast Upload Extension（Swift）
- `broadcastStarted` 初始化 Vision `VNRecognizeTextRequest`（内置模型，无需下载，不胀 ipa）。
- `processSampleBuffer`：把 `CMSampleBuffer` 转 `CGImage` → 送 Vision 识别 → 取 `topCandidates` 文本。
- **节流**：用时间戳，≥1s 或图像哈希变化才识别一次，避免每帧 OCR 烧电。
- 识别结果拼接成「题目文本」→ 写入 App Group 文件 `ocr_latest.txt`（含时间戳）。
- 不需要麦克风/摄像头权限；扩展仅处理屏幕帧。

### 3.2 App Group 共享区（主 App ↔ 扩展）
- 标识符：`group.com.<yourid>.qbank`（实现时替换）。
- 文件：
  - `ocr_latest.txt`：扩展写，主 App 读（最新识别到的题目文字）。
  - `bank.json`：主 App 在开始扫描时写（序列化当前题库 Questions）。
  - `answer_latest.txt`：主 App 写，扩展/备用读（top 答案文本，便于调试）。
- 用 `FileManager.containerURL(forSecurityApplicationGroupIdentifier:)` 访问。

### 3.3 主 App 匹配 + PiP 悬浮窗
- 「开始扫描」：① 把题库序列化写 App Group；② 启动 PiP（`AVPictureInPictureController` + `contentSource` 指向答案 UIView）；③ 弹出 `RPSystemBroadcastPickerView` 让用户点「开始直播」。
- PiP 常驻期间：主 App 起一个 `Timer`（~500ms）轮询 `ocr_latest.txt` → 调用 Dart 侧 `SearchIndex.search()` → 把 top 答案（题目 + 正确答案 + 高亮选项）写入答案 UIView 并刷新 PiP。
- 「停止扫描」：停 PiP + 停 Timer +（用户可在控制中心停录屏）。
- 答案 UIView：简洁卡片——题目（截断）、大号正确答案、选项高亮（绿）。可拖动、可缩放（PiP 系统能力）。

### 3.4 Flutter 侧
- `scan_bridge.dart`：`MethodChannel('qbank/scan')` 暴露 `startScan`/`stopScan`，并通过 `EventChannel` 把答案推给 Dart UI（用于非 PiP 场景/调试）。
- `search_screen.dart`：加扫描开关与状态；原「粘贴搜题 + 实况文本」**保留**作为无录屏时的兜底（比如不想开录屏权限时）。

---

## 4. 云端 CI 改动要点
- `flutter build ios --release --no-codesign` 会编译整个 Xcode workspace，**包含 Broadcast Upload Extension 目标**（只要扩展已加入 `.xcodeproj`）。
- 需确认：`--no-codesign` 对扩展目标同样不签名（扩展也需签名才能装，Sideloadly 侧载时统一用 Apple ID 签）。
- 原生自定义文件（扩展 swift / entitlements / Info.plist 改动）须在 `flutter create` 之后、build 之前**脚本化写入**（CI 里加一段 copy/patch），否则会被 `create` 覆盖。
- Artifacts 仍产出 `qbank-search.ipa`（Payload 含主 App + 扩展）。

---

## 5. 风险与坑
| 风险 | 说明 / 缓解 |
|---|---|
| iOS 版本 | 需 iOS 15+（UIView PiP）。低于则不支持，文档注明。 |
| PiP 后台定时器可靠性 | PiP 应保活主 App 跑 Timer；若不达标，回退方案：扩展内用 Swift 轻量移植 `similarity()` 直接匹配，主 App 仅渲染答案（加重构但更稳）。 |
| 一次系统确认 | 无法去除，属苹果强制；用户体验上已是「App 内点一下」。 |
| 性能/耗电 | OCR 节流（≥1s/变化才识别）；Vision 内置模型，无额外下载。 |
| 考试 App 反录屏 | iOS 极少有 App 禁录屏（无安卓 `FLAG_SECURE` 类硬屏蔽），通常可抓；若个别 App 屏蔽则抓不到（小概率）。 |
| 7 天签名过期 | 已有手册覆盖：考前重侧载，同 Apple ID 数据不丢。 |
| CI 覆盖原生文件 | `flutter create` 会清掉自定义原生文件 → 必须脚本化还原（同 `lib` 备份思路）。 |

---

## 6. 验证方式
1. 本地 `flutter analyze`（Dart 改动）+ Xcode 编译 iOS（需 macOS，本机无 → 靠云端 CI）。
2. 云端 CI：push → Run workflow → 下载 ipa（含扩展）。
3. Sideloadly 侧载 → iPhone 开开发者模式 → 信任证书。
4. 实测：打开任意含题页面 → App 内点「开始扫描」→ 系统确认 → 悬浮 PiP 窗出现并开始显示答案 → 停止扫描。
5. 兜底：关闭录屏权限时，原「粘贴搜题」仍可用。

---

## 7. 实施步骤顺序（待你确认后开工）
1. 加 App Group + entitlements + Broadcast Upload Extension 目标（原生脚手架）。
2. 写 Extension OCR → App Group。
3. 写主 App PiP 悬浮窗 + 轮询匹配（复用 Dart 搜索）。
4. Flutter 侧 `scan_bridge` + `search_screen` 按钮。
5. 改 CI 脚本化写入原生文件 + 验证 build。
6. 更新装机手册「录屏搜题」段。
7. 推 GitHub → 云端构建 → 交付 ipa。

---

### 一句话总结
iPhone 上「悬浮窗自动扫描」= **Broadcast 扩展做 OCR + PiP 小窗显示答案**；开头一次系统确认无法省，之后全自动；现有 Dart 搜索逻辑复用，不重写第二套。方案可行，等你点头我就按第 7 节顺序开工。
