# 把「搜答案」装到 iPhone 上 —— 完整步骤

全程不需要 Mac，不需要写代码。总耗时约 40 分钟，其中大部分是等待。

流程一共四段：

```
①  代码传到 GitHub          （约 10 分钟，只做一次）
②  点一下按钮，云端 Mac 帮你编译，下载 ipa   （约 15 分钟，每次改代码重来一遍）
③  电脑装 Sideloadly，把 ipa 签名装进手机     （约 10 分钟，首次配置）
④  手机上信任证书 + 导入题库                （约 5 分钟）
```

---

## 第一段：把代码传到 GitHub

### 1.1 注册账号

打开 https://github.com/signup ，用邮箱注册。免费。

### 1.2 新建仓库

登录后点右上角 `+` → `New repository`。

| 填什么 | 值 |
|---|---|
| Repository name | `qbank-search` |
| 可见性 | **选 Public（公开）** |
| 其它 | 全部留空，不要勾 Add a README |

点 `Create repository`。

> **为什么必须选 Public？**
> GitHub 给公开仓库的 macOS 云端编译是**完全免费、不限次数**的；私有仓库每月只有约 200 分钟 macOS 额度（一次编译约 10 分钟，等于每月只能构建 20 次）。
> 这个仓库里**只有 App 的程序代码，没有你的题库**——题库是你之后在手机上自己导入的，永远只存在你手机里，不会上传。所以设成 Public 没有任何隐私问题。

### 1.3 上传文件

在刚创建的空仓库页面，点 `uploading an existing file` 链接。

然后打开电脑文件夹 `C:\Users\Administrator\Projects\qbank-search`，**全选里面的所有内容**（包括 `.github` 这个文件夹、`app` 文件夹、`codemagic.yaml` 等），拖进网页的上传区。

等文件列表都出现后，翻到页面底部点绿色的 `Commit changes`。

> 拖拽时如果 `.github` 文件夹没传上去（有些浏览器会跳过点开头的文件夹），用下面的命令行方式代替，或者手动补：点 `Add file` → `Create new file` → 文件名栏输入 `.github/workflows/build.yml`（输入斜杠会自动变成目录），然后把本地那个文件的内容全部复制粘贴进去，保存。

**命令行方式（更省事，代码已经准备好，直接复制整段粘贴到终端）**：

```bash
cd /c/Users/Administrator/Projects/qbank-search
git init
git add -A
git commit -m "题库搜答案 App"
git branch -M main
git remote add origin https://github.com/你的用户名/qbank-search.git
git push -u origin main
```

推送时会让你登录，密码栏要填 **Personal Access Token** 而不是账号密码（GitHub 现在不接受密码了）：
头像 → Settings → 最底下 Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic) → 勾选 `repo` → 生成后复制那串字符，粘到密码栏。

---

## 第二段：云端编译，拿到 ipa

### 2.1 启动构建

仓库页面顶部点 `Actions` 标签。

第一次进会提示 "Workflows aren't being run on this forked repository" 或让你确认启用，点绿色按钮 `I understand my workflows, go ahead and enable them`。

左侧列表点 **「构建安装包」** → 右侧 `Run workflow` 下拉 → 再点绿色 `Run workflow` 按钮。

### 2.2 等待

页面会出现一条黄色的运行记录，点进去能看到三个任务：

| 任务 | 干什么 | 大约耗时 |
|---|---|---|
| 代码检查 | 先用便宜的 Linux 机器检查语法错误 | 2 分钟 |
| iOS (未签名 ipa) | 云端真 Mac 编译 | 8–12 分钟 |
| Android (apk) | 顺带出个安卓包 | 5 分钟 |

**「代码检查」故意排在最前面**——语法有问题时它 2 分钟就报错，不会浪费后面昂贵的 macOS 时间。

全部变绿勾就是成功了。

### 2.3 下载

在这个运行记录页面往下拉，最底下有 `Artifacts` 区域，里面有：

- `qbank-search-ipa` ← 这是 iPhone 要用的
- `qbank-search-apk` ← 安卓的，你用不上

点 `qbank-search-ipa` 下载，得到一个 zip，解压出 `qbank-search.ipa`。

> 如果构建失败（红色叉），点进失败的那个任务能看到红色的报错行。把报错信息发给我，我改代码后你重新 Run workflow 就行。

---

## 第三段：用 Sideloadly 装进手机

### 3.1 先装两个前置软件（这一步最容易踩坑）

Sideloadly 需要苹果的驱动组件，**必须装官网下载的 exe 版本，不能用微软商店版**：

1. iTunes：https://www.apple.com/itunes/download/win64 （下载 exe 安装）
2. iCloud for Windows：苹果官网下载 exe 版

> **如果你之前从 Microsoft Store 装过 iTunes 或 iCloud，必须先卸载干净再装 exe 版**，否则 Sideloadly 会一直识别不到你的 iPhone。这是最常见的失败原因。

### 3.2 装 Sideloadly

官网 https://sideloadly.io/ ，下 Windows 版，免费。

### 3.3 手机准备

iPhone 上：设置 → 隐私与安全性 → 拉到最底下 → **开发者模式** 打开 → 重启手机。

（iOS 16 以前的系统没有这个选项，跳过。）

### 3.4 签名安装

1. 数据线连电脑，手机上弹出「信任此电脑」点信任
2. 打开 Sideloadly，`iDevice` 栏应该出现你的 iPhone 型号
3. 把 `qbank-search.ipa` 拖进左边的 IPA 框
4. `Apple ID` 栏填你的 Apple ID 邮箱
5. 点 `Start`，输密码，有两步验证就输 6 位码
6. 进度条走完显示 `Done`，手机桌面就多了个「搜答案」图标

> **建议用一个专门注册的小号 Apple ID 做这件事**，不用你日常的主 ID。注册一个新 Apple ID 是免费的，不用绑卡。

### 3.5 信任证书

这时候点图标会提示「不受信任的开发者」，正常的。

手机上：设置 → 通用 → **VPN与设备管理** → 在「开发者App」下找到你的 Apple ID → 点进去 → **信任**。

回桌面再点，就能打开了。

---

## 第四段：导入题库，开始用

### 4.1 把题库放进手机

把 `新2025强基练兵题库单选、多选、判断.xlsx` 传到手机的「文件」App 里。任选一种：

- 微信发给自己 → 点开文件 → 右上角 `...` → 用其他应用打开 → 存储到「文件」
- 数据线连电脑，用 iTunes 文件共享拷进去
- 存到 iCloud Drive / 百度网盘，手机上下载到「文件」

### 4.2 导入

打开「搜答案」→ 底部选「导入」→ 点「选择题库文件」→ 在文件浏览器里找到那个 xlsx → 选中。

几秒后显示：`导入成功，共 1453 道 / 单选题 869 · 多选题 255 · 判断题 329`

**换题库随时重新导一次就行**，会整库替换，不用管旧的。

### 4.3 考试时怎么用

这是最关键的部分，实际操作只有三步：

```
① 在考试页面截图（同时按 电源键 + 音量加）
② 左下角弹出的截图缩略图点开 → 在题目文字上长按 → 拖动选中整道题 → 拷贝
   （这是 iOS 15+ 系统自带的「实况文本」，中文识别精度比第三方 OCR App 更好）
③ 切到「搜答案」→ 答案已经出来了
```

第③步不用做任何操作——App 检测到剪贴板内容变了会自动搜索。如果没自动出，点一下「粘贴搜题」按钮。

答案区显示三条候选，**第一条是最匹配的**，用大字显示答案，下面列出全部选项、命中的那个高亮。旁边的百分比是匹配度，95% 以上基本就是原题。

**为什么不做 App 内自动截屏识别**：那需要 App 常驻后台持续读屏，是考试平台反作弊检测最容易抓到的行为。用系统截图 + 系统文字识别，你的操作在系统层面和"截个图"没区别，同时识别精度还更高。

> 想要**完全不用手动截图、自动扫描**？见下面的「第五段：录屏悬浮自动扫描」。

---

## 第五段：录屏悬浮自动扫描（iPhone 专属，iOS 15+）

如果你嫌每题截图复制太麻烦，可以开「录屏搜题」——点一下开始，之后**全自动**：系统把考试界面实时喂给 App，自动识别出题目、答案直接浮在考试界面上方的小窗里，你边看题边看答案。

**前提：iPhone 需 iOS 15 及以上**（"悬浮窗"在 iPhone 上本质是画中画 PiP 小窗，iOS 15 才开始支持在 PiP 里显示自定义内容）。低于 iOS 15 用不了这个模式，退回上面「粘贴搜题」即可。

### 怎么用（4 步）

```
① 在「搜答案」App 的「搜答案」页，点蓝色「开始扫描」按钮
② 弹出系统的「屏幕录制」选择框 → 在列表里选「搜答案」那一项 → 点「开始直播」
   （这一步是苹果隐私强制的，谁都去不掉，但就在 App 里点一下，不是每题手动截）
③ 切回考试 App 答题 —— 屏幕上会浮出一个小窗（可拖动），
   实时显示当前屏幕题目的答案；没识别到时会显示「录屏搜题中…」
④ 考完或不想用了，回「搜答案」点「停止扫描」即可
```

### 几个要点

- **开头那次确认去不掉**，但只用点一次；之后全自动，不需要每题操作。
- 悬浮窗是系统画中画小窗，**可以拖动位置**；App 在后台靠一段无声音频保持活跃，所以答案能持续刷新（多耗一点电，一场考试完全够用）。
- 扩展（录屏识别）和主 App 通过 **App Group** 共享数据。绝大多数免费 Apple ID 都支持 App Group，正常能用。

### 兜底（很重要）

如果侧载后点「开始扫描」**悬浮窗不显示答案**、或报 App Group /  entitlements 相关错误，基本是免费 Apple ID 的 App Group 能力没生效。这时**退回「粘贴搜题」**（前面第四段③的截图→长按复制→自动出答案）照样能考，不影响使用。两种模式都在 App 里，随时切换。

---

## 关于 7 天过期（重要）

免费 Apple ID 签名的 App **7 天后会失效**，点开会闪退或提示无法验证。这是苹果对免费账号的硬性限制，任何工具都绕不过。

三个应对方案：

| 方案 | 做法 | 成本 |
|---|---|---|
| **考前手动重签**（推荐） | 考试前一天，插上数据线重跑一遍 Sideloadly，2 分钟搞定 | 免费 |
| AltStore 自动续签 | 装 AltServer 在电脑上，手机和电脑同一 WiFi 时自动续期 | 免费，但要电脑常开 |
| 买开发者账号 | Apple Developer $99/年，签名有效期变 1 年 | 约 700 元/年 |

**重签不会丢题库**：只要用同一个 Apple ID 重新安装同一个 ipa，iOS 视为覆盖升级，本地数据库保留。但如果你换了 Apple ID 签名，Bundle ID 会变，数据会丢——那就重新导一次题库，几秒钟的事。

> 养成习惯：**考试前一天晚上重签一次**，就永远不会出问题。

---

## 常见问题

**Sideloadly 找不到我的 iPhone**
99% 是 iTunes/iCloud 装成了微软商店版。卸载干净，重装官网 exe 版。

**提示 "Guru Meditation" 或签名失败**
免费 Apple ID 每周最多创建 10 个 App ID，同时最多 3 个自签 App。删掉手机上其他侧载的 App 再试，或等一周。

**构建失败，红色叉**
点进去看报错，把红色那几行发我。改完代码重新 push，再 Run workflow。

**App 打开是白屏**
第一次启动要初始化数据库，等两秒。还不行就删了重装。

**搜出来的答案不对**
看匹配度百分比。低于 60% 说明题库里可能没这道题，或者复制的文字太少。多选中一些文字再试。三条候选里可以人工挑一条。

**录屏扫描点了没反应 / 悬浮窗不显示答案**
免费 Apple ID 的 App Group 能力偶尔不生效，导致扩展和主 App 之间数据不通。先确认系统是 iOS 15+；若仍不行，退回「粘贴搜题」（截图→长按复制→自动出答案）一样能考。

**iOS 版本太低，没有「开始扫描」或 PiP 用不了**
录屏悬浮扫描需要 iOS 15+。低于此版本请只用「粘贴搜题」模式。

---

## 想改点什么

代码都在 `app/lib/` 下：

| 文件 | 管什么 |
|---|---|
| `services/search.dart` | 匹配算法，改相似度策略 |
| `services/parser.dart` | 题库表格怎么解析，改列名识别规则 |
| `services/xlsx.dart` | Excel 底层解析 |
| `screens/search_screen.dart` | 搜题界面、答案怎么显示 |
| `screens/import_screen.dart` | 导入界面 |

改完 push 到 GitHub，再 Run workflow 出新包。
